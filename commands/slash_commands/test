const {Command_template} = require("../../config/templates");
const Discord = require("discord.js");
const {time} = require("@discordjs/builders");

class Command extends Command_template {
  constructor(args, interaction) {
    super(interaction);
    Object.assign(this, args);

    this.options = {
      permissions: [],
      custom_perms: ["OWNER"],
      allowed_roles: [],
      slash: {
        name: "test",
        description: "test"
      }
    };
  }

  async execute() {
    // let dm = await this.interaction.user.createDM();
    let dm = this.interaction.user.dmChannel;
    //1643665973000
    console.log(time(1643665973000, "R"));
  }
}

module.exports = Command;
